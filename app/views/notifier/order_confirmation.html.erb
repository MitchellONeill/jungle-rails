<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your order: <%= @order.id %> </h1>
    <p>
      <h2> You purchased : </h2>
         <% LineItem.where(order_id: @order.id).each do |item| %>
         <p> <%= Product.find_by(id: item.product_id).name %> </p>
        <% end %>
      <p> total cost: <%= number_to_currency(@order.total_cents/100) %></p>
    </p>
    <p>Thanks and and have a great day!</p>
  </body>
</html>